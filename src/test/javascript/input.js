function myfunc(sd, sd, sd) {
// este es un comentario
    var variable1 = 0 || 0;
    var variable2 = 0 && "text" || 23
    const inlinegroups = (23 + 1) / 2 * 23 / 2 + 2, siuuf = 23

    const sdf = !(234 + 23) * (2) + 2;

    // uso de tipo let
    let variable3 = "hola", siuu = "nuevo"
    let variable4 = true;

    // uso de const
    const variable5 = 4 + 50.5 * s / 2 + 23
    const variable6 = 'x'

    siuuu(smec, "Sd");


    do {
        siuuu++
    } while (wd > 0)

    for (let z = 2, le = 23 + 2; siuu <= 2; siu + 8) {
        for (let z = 2; siuu <= 2; siu + 6) {

            while (!sumecha) {
                while (!sumecha) {
                    let vsd = require("ruta01");
                    console.log(ayno);
                }
            }
        }

        while (!(j != 0)) {
            //ejemplo 1
            switch (numero) {
                case 0:
// instrucciones
                    break;
                case 1:
// instrucciones
                    break;
                default:
                    siu + 1
// instrucciones
            }

        }



        if (!prs != se && 1 / 2 == 23 + 1 + 24 || s == 23 && 2 * 2 - !nd != 2 + 2) {
            // CONDICIONAL
            if (!prs != se) {
                break;

                while ((3 + s) == 23) {

                    break;


                }

                for (let z = 2; siuu <= 2 + 2; siu + 2) {

                    while (se >= 23 + 2) {
                        break;
                    }
                }
            }
        }
    }
}