class myfunc {
    nuevo2(siuu, s, s) {

        for (let z = 2, le = 23 + 2; siuu <= 2; siu + 8) {
            for (let z = 2; siuu <= 2; siu + 6) {

                while (!sumecha) {
                    while (!sumecha) {
                        let vsd = require("ruta01");
                        console.log(ayno);
                    }
                }
            }

            while (!(j != 0)) {
//ejemplo 1
                switch (numero) {
                    case 0:
// instrucciones1
                        break;
                    case 1:
// instrucciones1
                        break;
                    default:
                        siu + 1
// instrucciones1
                }

            }



            if (!prs != se && 1 / 2 == 23 + 1 + 24 || s == 23 && 2 * 2 - !nd != 2 + 2) {
// CONDICIONAL1
                if (!prs != se) {
                    break;
                    while ((3 + s) == 23) {

                        break;
                    }

                    for (let z = 2; siuu <= 2 + 2; siu + 2) {

                        while (se >= 23 + 2) {
                            break;
                        }
                    }
                }
            }
        }
    }
}
