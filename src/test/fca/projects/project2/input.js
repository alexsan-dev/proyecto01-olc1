class myfunc {
    nuevo3(siuu, s) {

        for (let z2 = 2, le4 = 23 + 2; siuu <= 2; siu + 8) {
            for (let z1 = 2; siuu <= 2; siu + 6) {

                while (!sumecha) {
                    while (!sumecha) {
                        let vsd3 = require("ruta01");
                        console.log(ayno);
                    }
                }
            }

            while (!(j != 0)) {
//ejemplo 1
                switch (numero) {
                    case 0:
// instrucciones
                        break;
                    case 1:
// instrucciones
                        break;
                    default:
                        siu + 1
// instrucciones
                }

            }



            if (!prs != se && 1 / 2 == 23 + 1 + 24 || s == 23 && 2 * 2 - !nd != 2 + 2) {
// CONDICIONAL
                if (!prs != se) {
                    break;
                    while ((3 + s) == 23) {

                        break;
                    }

                    for (let z5h = 2; siuu <= 2 + 2; siu + 2) {

                        while (se >= 23 + 2) {
                            break;
                        }
                    }
                }
            }
        }
    }
}
